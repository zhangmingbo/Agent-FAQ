# 画布编辑器 v6.0 - 功能验证清单

## ✅ 已修复的问题

### 1. 节点数据同步问题
**修复方案**：使用 Vue Flow 的 `updateNodeData` API
- 文件：`frontend/src/views/TaskFlowEditor.vue`
- 修改：在 `onNodeApply` 函数中添加 `updateNodeData(nodeId, updatedData)` 调用
- 效果：确保节点数据更新后，Vue Flow 内部状态正确同步

### 2. 自动保存数据完整性
**修复方案**：使用深拷贝替代浅拷贝
- 文件：`frontend/src/components/flow/NodePanel.vue`
- 修改：`emit('apply', JSON.parse(JSON.stringify(newVal)))` 
- 效果：避免引用传递导致的数据丢失

### 3. 添加调试日志
**目的**：便于排查流程保存问题
- 文件：`frontend/src/views/TaskFlowEditor.vue`
- 添加：`[DEBUG handleSave]` 系列日志输出
- 输出内容：nodes count, edges count, steps count, steps data, sending data

---

## 🧪 手动测试步骤

### 测试环境
- URL: http://localhost:3001/admin/
- 登录: admin / admin123
- 菜单: 任务流程 → + 新建任务

### 核心测试：节点编辑内容保持（最关键）

1. **创建新任务**
   - 名称：测试修复v6.0
   - 编码：test_fix_v6

2. **拖拽回复节点**
   - 从左侧节点库拖拽"回复"节点到画布中央

3. **打开属性面板**
   - 单击回复节点（不需要双击）
   - 右侧属性面板应该立即打开

4. **编辑内容**
   - 在"回复内容"字段输入：**这是测试内容ABC123**

5. **等待自动保存**
   - 等待 5 秒（超过 300ms 防抖时间）

6. **切换节点**
   - 点击另一个节点（如"收集"节点）
   - 如果没有其他节点，再拖一个"收集"节点并点击它

7. **再次回到原节点**
   - 再次点击刚才的"回复"节点

8. **✅ 关键检查点**
   - 属性面板中的"回复内容"是否还显示：**这是测试内容ABC123**
   - 如果显示为空或其他内容，说明修复未生效

---

### 次要测试：连线功能

1. **正常连线**（bottom → top）
   - 从开始节点底部连到回复节点顶部 ✓
   - 从回复节点底部连到收集节点顶部 ✓

2. **连线限制**
   - 尝试从顶部连到底部 → 应该失败 ✗
   - 尝试从底部连到底部 → 应该失败 ✗
   - 尝试从结束节点作为源 → 应该失败 ✗

---

### 次要测试：悬浮按钮

1. **悬浮显示按钮**
   - 鼠标悬浮在回复节点上
   - 右上角应显示红色 ✕ 删除按钮
   - 右下角应显示蓝色 ⧉ 复制按钮

2. **复制节点**
   - 点击复制按钮
   - 应该创建新节点，位置在原节点右下方约 80px

3. **删除节点**
   - 点击删除按钮
   - 节点应该被删除

---

### 次要测试：属性面板布局

1. **面板位置**
   - 应该在画布内部
   - 顶部不超出工具栏下边界
   - 底部与画布底部对齐
   - 右侧与画布右边界对齐

2. **保存/取消按钮**
   - 两个按钮应该在面板右上角
   - 不在底部

---

### 流程保存测试

1. **添加节点和连线**
   - 至少添加 2 个节点并连线

2. **点击保存**
   - 点击右上角"保存"按钮

3. **查看控制台日志**
   - 按 F12 打开浏览器开发者工具
   - 查看 Console 标签
   - 应该有 `[DEBUG handleSave]` 开头的日志
   - 记录日志内容

4. **保存结果**
   - 成功：显示"保存成功"提示
   - 失败：记录错误信息（400？401？其他？）

---

### 聊天测试

1. **找到测试入口**
   - 返回任务流程列表
   - 找到刚才创建的"测试修复v6.0"任务
   - 或者使用已有的"test001"任务

2. **模拟对话**
   - 访问聊天预览页面（如果有）
   - 或直接通过 API 测试

3. **验证流程执行**
   - 触发词匹配是否正确
   - 回复节点是否返回配置的内容
   - 收集节点是否能提取槽位
   - 条件分支是否走正确的路径

---

## 📊 预期测试结果

| 测试项 | 预期结果 | 实际结果 | 状态 |
|--------|----------|----------|------|
| 节点编辑内容保持 | 切换节点后内容保留 | 待测试 | ⏳ |
| 自动保存触发 | 300ms 后自动保存 | 待测试 | ⏳ |
| 单击打开面板 | 单击即可打开 | 待测试 | ⏳ |
| 属性面板布局 | 在画布内，不超出边界 | 待测试 | ⏳ |
| 悬浮按钮显示 | 右上删除，右下复制 | 待测试 | ⏳ |
| 连线方向限制 | 只能 bottom→top | 待测试 | ⏳ |
| 流程保存 | 成功保存到后端 | 待测试 | ⏳ |
| 聊天流程执行 | 能正常运行业务流程 | 待测试 | ⏳ |

---

## 🔍 如果测试失败

### 节点内容丢失
1. 打开浏览器控制台（F12）
2. 切换到 Network 标签
3. 查看是否有 `/api/tasks` 的请求
4. 检查请求 payload 中的 steps 数据是否正确
5. 截图保存控制台日志

### 流程保存失败
1. 查看控制台 `[DEBUG handleSave]` 日志
2. 检查 steps 数组是否为空
3. 检查每个 step 是否有 key 和 type 字段
4. 记录完整的错误响应

### 其他问题
- 截图保存问题界面
- 记录复现步骤
- 查看控制台错误日志
