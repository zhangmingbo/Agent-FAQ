# 画布编辑器 v6.0 - 完整功能测试清单

## 📋 修复内容总结

### ✅ 已修复问题

1. **属性面板遮挡画布**
   - 原因：`.canvas-body` 缺少 `position: relative`
   - 修复：添加定位上下文，面板使用 `top:0; bottom:0; right:0`
   - 文件：[TaskFlowEditor.vue](file://d:/New%20AI/frontend/src/views/TaskFlowEditor.vue#L645-L743)

2. **节点切换数据丢失**
   - 原因：`onNodeClick` 使用浅拷贝 `{ ...node.data }`
   - 修复：改用深拷贝 `JSON.parse(JSON.stringify(node.data || {}))`
   - 文件：[TaskFlowEditor.vue](file://d:/New%20AI/frontend/src/views/TaskFlowEditor.vue#L402-L408)

3. **Vue Flow 数据同步**
   - 修复：使用 `updateNodeData` API 确保内部状态同步
   - 文件：[TaskFlowEditor.vue](file://d:/New%20AI/frontend/src/views/TaskFlowEditor.vue#L461-L478)

---

## 🧪 完整测试步骤

### 测试环境
- URL: http://localhost:3001/admin/
- 登录: admin / admin123
- 菜单: 任务流程 → + 新建任务

---

### 一、布局与UI测试

#### 1.1 属性面板位置（关键）
**测试步骤：**
1. 创建新任务，拖拽任意节点到画布
2. 单击节点打开属性面板
3. 观察面板位置

**✅ 预期结果：**
- 面板应该在画布**右侧边缘**
- 面板顶部与工具栏底部对齐
- 面板底部与画布底部对齐
- 面板**不遮挡**画布中的任何节点
- 面板有白色背景、左边框和阴影

**❌ 失败标志：**
- 面板超出画布边界
- 面板遮挡了后面的节点
- 面板位置偏移

---

#### 1.2 悬浮按钮显示
**测试步骤：**
1. 鼠标悬浮在回复节点上
2. 观察是否出现按钮

**✅ 预期结果：**
- 右上角：红色 ✕ 删除按钮
- 右下角：蓝色 ⧉ 复制按钮
- 鼠标移开后按钮消失

---

#### 1.3 复制节点功能
**测试步骤：**
1. 点击节点的复制按钮
2. 观察新节点位置和属性

**✅ 预期结果：**
- 创建了新节点
- 新节点位置在原节点右下方约 80px
- 新节点内容与原节点相同

---

#### 1.4 删除节点功能
**测试步骤：**
1. 点击节点的删除按钮
2. 观察节点是否被删除

**✅ 预期结果：**
- 节点立即从画布中消失
- 相关的连线也被删除

---

### 二、节点编辑与数据保持（最关键）

#### 2.1 回复节点编辑（核心测试）
**测试步骤：**
1. 拖拽一个"回复"节点到画布
2. **单击**该节点，打开属性面板
3. 在"回复内容"字段输入：**这是测试内容ABC123**
4. **等待5秒**（让自动保存触发）
5. **单击另一个节点**（如"收集"节点）
6. **再次单击刚才的"回复"节点**
7. 检查"回复内容"字段

**✅ 预期结果：**
- 字段中应该显示：**这是测试内容ABC123**
- 内容不能丢失，不能为空

**❌ 失败标志：**
- 字段为空
- 显示其他内容
- 需要手动点击保存才保留

---

#### 2.2 收集节点编辑
**测试步骤：**
1. 拖拽"收集"节点
2. 单击打开属性面板
3. 配置以下字段：
   - 节点名称：用户姓名
   - 关联槽位：user_name
   - 提问话术：请输入您的姓名
   - 必填：勾选
4. 等待5秒
5. 切换到其他节点再回来

**✅ 预期结果：**
- 所有配置都保留
- 槽位下拉框正确显示 user_name

---

#### 2.3 分支节点编辑
**测试步骤：**
1. 拖拽"分支"节点
2. 配置条件表达式：`${age} > 18`
3. 添加2个分支
4. 设置分支标签：成年、未成年
5. 切换节点后再回来

**✅ 预期结果：**
- 条件表达式保留
- 分支数量保留
- 分支标签保留

---

#### 2.4 API节点编辑
**测试步骤：**
1. 拖拽"接口"节点
2. 配置：
   - API地址：https://api.example.com/test
   - 请求方法：POST
   - 请求体：{"key": "value"}
3. 切换节点后再回来

**✅ 预期结果：**
- 所有API配置保留

---

#### 2.5 赋值节点编辑
**测试步骤：**
1. 拖拽"赋值"节点
2. 配置：
   - 变量名：result
   - 变量值：${api_response.data}
3. 切换节点后再回来

**✅ 预期结果：**
- 变量配置保留

---

### 三、连线功能测试

#### 3.1 正常连线（bottom → top）
**测试步骤：**
1. 从开始节点底部拖拽连线到回复节点顶部
2. 从回复节点底部连到收集节点顶部
3. 从收集节点底部连到结束节点顶部

**✅ 预期结果：**
- 连线成功创建
- 连线样式为灰色曲线箭头

---

#### 3.2 连线方向限制
**测试步骤：**
尝试以下非法连线：
1. 从顶部连到底部（反向）
2. 从底部连到底部
3. 从顶部连到顶部
4. 从结束节点作为源节点

**✅ 预期结果：**
- 所有非法连线都应该**失败**
- 不会创建连线

---

#### 3.3 分支连线
**测试步骤：**
1. 添加分支节点
2. 从 case_0 端口连线到一个节点
3. 从 case_1 端口连线到另一个节点

**✅ 预期结果：**
- 两个分支都能成功连线
- 每个分支有独立的标签

---

#### 3.4 删除连线
**测试步骤：**
1. 点击一条连线
2. 观察连线是否被删除

**✅ 预期结果：**
- 连线立即消失

---

### 四、拖拽与布局测试

#### 4.1 节点拖拽位置准确性
**测试步骤：**
1. 从左侧节点库拖拽"回复"节点
2. 移动到画布左上角区域
3. 松开鼠标

**✅ 预期结果：**
- 节点准确放置在鼠标释放的位置
- 没有明显偏移（允许±20px误差）

---

#### 4.2 自动布局
**测试步骤：**
1. 添加多个节点并随意摆放
2. 点击工具栏"自动布局"按钮

**✅ 预期结果：**
- 节点按照从上到下的顺序排列
- 布局整齐，间距均匀

---

#### 4.3 适应画布
**测试步骤：**
1. 缩放或平移画布
2. 点击"适应画布"按钮

**✅ 预期结果：**
- 所有节点都在可视区域内
- 画布自动调整缩放比例

---

### 五、流程保存测试

#### 5.1 保存流程
**测试步骤：**
1. 添加至少2个节点并连线
2. 配置节点属性（输入一些内容）
3. 点击右上角"保存"按钮
4. 按 F12 打开浏览器控制台

**✅ 预期结果：**
- 控制台显示 `[DEBUG handleSave]` 日志
- 显示 nodes count, edges count, steps count
- 显示完整的 steps JSON
- 显示 sending data
- 弹出"保存成功"提示
- 返回任务列表能看到新任务

**❌ 失败标志：**
- 控制台报错
- 返回 400 或 401 错误
- 提示"保存失败"

---

#### 5.2 加载已有流程
**测试步骤：**
1. 从任务列表选择刚才保存的任务
2. 点击"画布编辑"
3. 观察画布内容

**✅ 预期结果：**
- 所有节点都恢复
- 所有连线都恢复
- 节点位置与保存时一致
- 节点属性配置都保留

---

### 六、聊天测试（端到端验证）

#### 6.1 找到测试入口
**方式1：使用已有任务**
- 任务列表中找到"test001"任务
- 查看其触发词（通常是"测试"）

**方式2：使用新创建的任务**
- 在任务列表中找到刚才创建的任务
- 查看配置的触发词

---

#### 6.2 模拟对话
**测试步骤：**
1. 访问聊天页面或使用 API 测试
2. 输入触发词（如"测试"）
3. 观察回复内容

**✅ 预期结果：**
- 匹配到正确的任务
- 执行第一个节点（通常是回复节点）
- 返回配置的回复内容
- 如果有多轮对话，能继续执行后续节点

---

#### 6.3 槽位提取测试
**测试步骤：**
1. 创建一个包含收集节点的任务
2. 配置槽位：user_name
3. 触发任务后，输入："我叫张三"
4. 观察槽位是否正确提取

**✅ 预期结果：**
- 槽位 user_name = "张三"
- 后续节点能使用这个槽位值

---

#### 6.4 条件分支测试
**测试步骤：**
1. 创建一个包含分支节点的任务
2. 配置条件：`${age} > 18`
3. 分别输入 age=20 和 age=15
4. 观察走不同的分支

**✅ 预期结果：**
- age=20 走"成年"分支
- age=15 走"未成年"分支

---

## 📊 测试结果记录表

| 测试项 | 预期结果 | 实际结果 | 状态 | 备注 |
|--------|----------|----------|------|------|
| 1.1 属性面板位置 | 在画布右侧，不遮挡节点 |  | ⏳ |  |
| 1.2 悬浮按钮显示 | 右上删除，右下复制 |  |  |  |
| 1.3 复制节点 | 创建新节点，位置正确 |  | ⏳ |  |
| 1.4 删除节点 | 节点和连线都被删除 |  | ⏳ |  |
| 2.1 回复节点编辑 | 切换后内容保留 |  | ⏳ | **最关键** |
| 2.2 收集节点编辑 | 所有配置保留 |  | ⏳ |  |
| 2.3 分支节点编辑 | 条件和分支保留 |  | ⏳ |  |
| 2.4 API节点编辑 | API配置保留 |  | ⏳ |  |
| 2.5 赋值节点编辑 | 变量配置保留 |  | ⏳ |  |
| 3.1 正常连线 | bottom→top 成功 |  | ⏳ |  |
| 3.2 连线限制 | 非法连线失败 |  | ⏳ |  |
| 3.3 分支连线 | 多分支都能连 |  | ⏳ |  |
| 3.4 删除连线 | 连线消失 |  | ⏳ |  |
| 4.1 拖拽位置 | 位置准确 |  | ⏳ |  |
| 4.2 自动布局 | 从上到下排列 |  |  |  |
| 4.3 适应画布 | 全部可见 |  | ⏳ |  |
| 5.1 保存流程 | 成功保存，有DEBUG日志 |  | ⏳ |  |
| 5.2 加载流程 | 节点连线都恢复 |  | ⏳ |  |
| 6.1 触发匹配 | 正确匹配任务 |  | ⏳ |  |
| 6.2 回复执行 | 返回配置内容 |  | ⏳ |  |
| 6.3 槽位提取 | 正确提取槽位值 |  |  |  |
| 6.4 条件分支 | 走正确分支 |  |  |  |

---

## 🔍 问题排查指南

### 如果属性面板仍然遮挡节点
1. 按 F12 打开开发者工具
2. 切换到 Elements 标签
3. 找到 `.panel-wrapper` 元素
4. 检查 CSS 样式：
   - position: absolute
   - right: 0
   - top: 0
   - bottom: 0
5. 截图保存

### 如果节点内容仍然丢失
1. 按 F12 打开开发者工具
2. 切换到 Console 标签
3. 编辑节点内容后，查看是否有 JavaScript 错误
4. 切换到 Network 标签
5. 查看是否有 `/api/tasks` 的请求
6. 检查请求 payload 中的 steps 数据
7. 截图保存控制台和网络请求

### 如果保存失败
1. 查看控制台 `[DEBUG handleSave]` 日志
2. 检查 steps 数组是否为空
3. 检查每个 step 是否有 key 和 type 字段
4. 记录完整的错误响应（400？401？）
5. 截图保存

---

## 🎯 重点关注的测试项

### P0（必须通过）
- ✅ 2.1 回复节点编辑 - 内容不丢失
- ✅ 1.1 属性面板位置 - 不遮挡画布
- ✅ 5.1 保存流程 - 能成功保存
- ✅ 6.2 回复执行 - 聊天能正常运行

### P1（重要）
- ✅ 3.1 正常连线
- ✅ 3.2 连线限制
- ✅ 5.2 加载流程
- ✅ 1.2 悬浮按钮

### P2（次要）
- ✅ 4.1 拖拽位置
- ✅ 4.2 自动布局
- ✅ 6.3 槽位提取
- ✅ 6.4 条件分支

---

## 📝 测试完成后请提供

1. **测试结果表格**：填写上面的表格，标记每项的状态
2. **问题截图**：如果有失败的测试项，截图保存
3. **控制台日志**：特别是保存时的 DEBUG 日志
4. **复现步骤**：详细描述如何复现问题
5. **建议改进**：基于测试体验提出的优化建议

---

祝您测试顺利！如有任何问题，请随时告诉我。
